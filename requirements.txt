# -- Database --
psycopg[binary]>=3.1.0

# -- Environment --
python-dotenv>=1.0.0

# -- LLM / Agent --
openai>=2.0.0
langgraph>=1.1
langgraph-checkpoint-postgres>=3.0
typing-extensions>=4.0

# -- API Server --
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# -- HTTP requests (Professor Fetcher / SerpAPI) --
requests>=2.28.0

# -- Crawler --
playwright>=1.40.0
# After install run: playwright install chromium
beautifulsoup4>=4.12   # GradCafe 爬蟲解析 data-page（含 agent 背景補爬）

# -- data_crawler pipeline (LangGraph 資料擷取) --
trafilatura>=2.0            # DOM → markdown
pymupdf>=1.24               # PDF 抽文字
langchain-text-splitters>=1.0   # chunking (Node 11)
sentence-transformers>=5.0  # embedding（ENABLE_EMBEDDING / backfill 時才需要）
