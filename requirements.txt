# -- Database --
psycopg[binary]>=3.1.0

# -- Environment --
python-dotenv>=1.0.0

# -- LLM / Agent --
openai>=1.40.0
langgraph>=0.2.0

# -- API Server --
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# -- HTTP requests (Professor Fetcher / SerpAPI) --
requests>=2.28.0

# -- Crawler --
playwright>=1.40.0
# After install run: playwright install chromium
